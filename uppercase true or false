#include <string>

bool is_uppercase(const std::string &s) {
bool lower;
  for (int i=0;i<s.length();i++)
  {
    lower=islower(s[i]); //will detect any lower cases in sentences or words

  if(lower==true)
 {
    return false;
    break;
  }
  
}
  return true;

  }
